[project]
name = "fundata"
version = "1.0.1"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "pandas",
    "funshell>=1.0.0",
    "funutil>=1.0.14",
    "tqdm",
]

[dependency-groups]
dev = [
    "pytest>=7.0",
]
